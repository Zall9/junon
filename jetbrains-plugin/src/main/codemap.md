# jetbrains-plugin/src/main/

## Responsibility

A path segment. Nothing lives here directly — Gradle separates a source set's languages and its resources.

## Design

Not a boundary in the system: no code, no decisions, no dependencies of its own.

## Flow

Continue in [`kotlin/`](kotlin/codemap.md).

## Integration

None. This directory exists because Gradle separates a source set's languages and its resources.
