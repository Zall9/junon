# …/test/com/idebridge/

## Responsibility

A path segment. Nothing lives here directly — the JVM requires a directory per package segment.

## Design

Not a boundary in the system: no code, no decisions, no dependencies of its own.

## Flow

Continue in [`jetbrains/`](jetbrains/codemap.md).

## Integration

None. This directory exists because the JVM requires a directory per package segment.
