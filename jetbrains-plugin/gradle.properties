pluginName=ide-bridge-jetbrains
# The distribution name above identifies the artifact; this is what a user sees in the IDE. They
# are separate because the Marketplace refuses a display name containing the word "JetBrains".
pluginDisplayName=IDE Bridge
pluginGroup=com.idebridge
pluginVersion=0.2.7
pluginSinceBuild=252

platformVersion=2025.2
platformType=IC

kotlin.stdlib.default.dependency=false
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
